package utils;

public class TestData {
    public static String registeredEmail;   // will be set dynamically
    public static String registeredPassword; // can be constant or dynamic

    // Invalid credentials (still static)
    public static String invalidEmail = "invalid@example.com";
    public static String invalidPassword = "WrongPass123";
}
